apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: heart-disease-ingress
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: heart-disease-service
            port:
              number: 8000
